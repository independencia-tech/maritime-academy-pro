# TODO

- ~~**SÉCURITÉ - AVANT LANCEMENT COMMERCIAL** : revoir GRANT UPDATE sur `user_profiles.tier`~~ — **Corrigé le 2026-09-07** (migration `20260907000000_harden_rls_grants.sql`, voir `audits/2026-09-07-audit-rls-supabase-suivi.md`). `tier` n'est plus modifiable par `authenticated`/`anon`, seul `service_role` le peut désormais.
- **Admin panel** (`AdminPanel.tsx`) reste un mock à durcir avant toute vraie monétisation — indépendamment du point ci-dessus, `grantPremium`/`revokePremium` n'ont jamais réellement fonctionné pour accorder du premium à un *autre* utilisateur (la policy RLS `UPDATE` de `user_profiles` scope à `auth.uid() = user_id`, donc l'appel de l'admin sur la ligne d'un autre utilisateur affecte 0 ligne en silence, sans erreur — confirmé lors du test de la migration ci-dessus). Un vrai flux d'attribution premium nécessitera soit un backend/edge function avec `service_role`, soit une fonction `SECURITY DEFINER` dédiée avec ses propres contrôles d'autorisation.
